#ifndef HELPERS_H
#define HELPERS_H
bool strings_equal(char* str1, char* str2);
char* get_next_cmd();
bool starts_with(char* prefix, char* string);
int min(int a, int b);
int max(int a, int b);
#endif